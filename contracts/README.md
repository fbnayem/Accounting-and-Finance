# Canonical Contracts

**Single source of truth** for the Accounting Platform's schema, events and API. Supersedes the
blueprints in `project plan/blueprints/`, which remain as the immutable v1.0 handoff and are no
longer authoritative.

Produced 2026-08-07 as Deliverable 2, resolving the 97 findings in [audit/](../audit/).

| Artifact                     | Supersedes                           | Contents                                |
| ---------------------------- | ------------------------------------ | --------------------------------------- |
| [schema/](schema/)           | `schema_blueprint.sql` (74 tables)   | 22 migrations, **237 tables**           |
| [events.yaml](events.yaml)   | `event_catalog.json` (136 events)    | **177 events**, 40 new, 12 renamed      |
| [openapi.yaml](openapi.yaml) | `api_route_catalog.yaml` (150 paths) | **202 paths / 260 operations**          |
| [../docs/adr/](../docs/adr/) | —                                    | 9 ADRs recording the ratified decisions |

## Verification status

All checks run against **PostgreSQL 16** in Docker (`postgres:16`).

```
ALL 22 MIGRATIONS APPLIED FORWARD ON AN EMPTY DATABASE

tables:      237      constraints: 1477
enum types:   61        foreign keys:  835
indexes:     543        check:         196
triggers:     27        unique:        185
functions:   288        exclusion:      18
```

Guard suite: [schema/tests/0006_guards_test.sql](schema/tests/0006_guards_test.sql) — **15/15 pass**.
Every rejection below is a defect the original blueprint permitted:

| Test                                                | Finding   | Result                                         |
| --------------------------------------------------- | --------- | ---------------------------------------------- |
| Overlapping fiscal year                             | F-015     | rejected by `fiscal_years_no_overlap`          |
| Overlapping regular period                          | F-011     | rejected by `accounting_periods_no_overlap`    |
| Adjustment period overlapping period 1              | F-011     | **accepted** (correct — that is their purpose) |
| Second primary book                                 | F-013     | rejected by `accounting_books_one_primary_idx` |
| Balanced journal with a zero-value line             | F-001     | **accepted** (the old CHECK rejected it)       |
| Unbalanced posted journal                           | F-005     | rejected at COMMIT by the deferred trigger     |
| Line referencing another entity's account           | **F-003** | rejected by `jl_account_scope_fk`              |
| UPDATE a posted journal line                        | **F-004** | rejected by `POSTED_IMMUTABLE`                 |
| DELETE a posted journal entry                       | **F-004** | rejected by `POSTED_IMMUTABLE`                 |
| POSTED → REVERSED                                   | doc 01 r4 | **accepted** (the one permitted transition)    |
| Overlapping posting rule versions                   | F-010     | rejected by `posting_rule_versions_no_overlap` |
| File key outside its tenant prefix                  | F-048     | rejected by `files_key_is_tenant_scoped`       |
| UPDATE an audit event                               | F-043     | rejected by `POSTED_IMMUTABLE`                 |
| Duplicate tenant-wide membership                    | F-017     | rejected by `NULLS NOT DISTINCT`               |
| Period resolution with an adjustment period present | F-403     | returns the regular period                     |

Contract validation:

```
events.yaml   177 events, 0 duplicates, 0 dropped from the original catalog,
              12 forbidden names all resolved, 0 naming-rule violations
openapi.yaml  260 operations, 0 duplicate operationIds, 0 missing x- markers,
              0 mutations without a permission, 0 unresolved $refs,
              188 distinct permissions (seeds the registry), 86 idempotency-required
```

## Traceability

[../audit/traceability.json](../audit/traceability.json) maps every finding to the artifacts that
resolve it, generated by scanning rather than asserted.

| Severity | Resolved in Deliverable 2 | Resolved in Phase 0 |              Still open |
| -------- | ------------------------: | ------------------: | ----------------------: |
| **S1**   |               **14 / 14** |                   — |                   **0** |
| S2       |                   35 / 36 |         1 (F-406\*) |                       0 |
| S3       |                   36 / 41 |           1 (F-312) |                       4 |
| S4       |                     0 / 6 |                   — | 6 (no action by design) |

\* The Deliverable 2 table counted F-406 among the S3 deferrals; it is S2. The totals are corrected
here rather than left to disagree with [05_Missing_Requirements.md](../audit/05_Missing_Requirements.md).

### Deferred, with reasons

Stating these rather than letting them disappear.

**Closed in Phase 0** — see [docs/PHASE_0.md](../docs/PHASE_0.md):

| ID        | Sev | Title                                 | Resolved by                                                                                                                                                                                         |
| --------- | --- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-406~~ | S2  | No accessibility standard             | [ADR-0010](../docs/adr/0010-accessibility-standard.md) — WCAG 2.2 AA, enforced by `packages/ui/src/contrast.test.ts` and the CI accessibility jobs. It caught a real 2.84:1 border on its first run |
| ~~F-312~~ | S3  | Definition of Done has no enforcement | [scripts/definition-of-done.mjs](../scripts/definition-of-done.mjs) — 8 mechanical checks in CI; the other 13 items print as the reviewer's list, so the machine/human boundary is explicit         |

**Closed in Phase 2** — see [docs/PHASE_2.md](../docs/PHASE_2.md):

| ID        | Sev | Title                                 | Resolved by                                                                                                                                                                                              |
| --------- | --- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~F-407~~ | S3  | Accounting grid behaviour unspecified | [apps/web/components/journal-grid.tsx](../apps/web/components/journal-grid.tsx) — the keyboard model, the running imbalance, and what happens on paste, all specified by being written down in one place |

Still open:

| ID                                       | Sev | Title                                                                                                     | Where it belongs                                                                                                                                   |
| ---------------------------------------- | --- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-502                                    | S3  | 12 `phases/*.md` files absent                                                                             | **External.** Request the complete package; doc 21 is self-sufficient meanwhile                                                                    |
| F-412                                    | S3  | No pricing / packaging / segment                                                                          | **Commercial.** The `entitlements` table is deliberately capability-keyed, not plan-name-keyed, so this can be decided later without schema change |
| F-413                                    | S3  | No customer-facing SLA or incident comms                                                                  | **Commercial.** `recovery_tiers` and `alert_thresholds` give it something to attach to                                                             |
| F-104, F-109, F-301, F-501, F-503, F-504 | S4  | Catalog grouping, naming inconsistency, positive finding, absent-but-inlined docs, README, manifest paths | **No action by design**                                                                                                                            |

## Findings raised after Deliverable 2

The audit read the specification. These were found by building against it, which is a different
instrument: each is a gap between two documents that only became visible when something had to
actually work. Numbered from F-601 to keep them distinct from the audit's own ranges, and resolved
in the phase that found them rather than deferred.

**Found in Phase 1** — see [docs/PHASE_1.md](../docs/PHASE_1.md):

| ID    | Finding                                                                                                                                                                                                                                                                                                                                                    | Resolution                                                                                                               |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| F-601 | The permission registry is generated from `x-permission`, so a permission that gates a _condition inside_ a route could never enter it. `postJournal`'s own description named `journal.post_control`, `createVendorBill`'s named `vendor_bill.override_duplicate`, and `tax_overrides` exists in the schema — none of the three could be granted or tested | `x-additional-permissions` on the operation. Five codes recovered, two of them among doc 02's seven high-risk categories |
| F-602 | ADR-0005 makes login a two-step flow and binds the tenant to the session, and no route performed step two                                                                                                                                                                                                                                                  | `POST /auth/tenants/{id}/select`                                                                                         |
| F-603 | doc 21 requires Phase 1 to deliver "tenant/account creation" and `tenant.created` exists as an event, but no route produced either — so doc 02's "reaches posting-ready status without direct DB work" had no first door                                                                                                                                   | `POST /auth/signup`                                                                                                      |
| F-604 | doc 02 requires an "invite/accept flow"; the catalog had the invite half only, so an invited user could never join                                                                                                                                                                                                                                         | `POST /auth/invitations/accept` and the `user_invitations` table (migration 0023)                                        |
| F-605 | doc 01's precedence chain has six levels and `branches` had no `settings` column, so level 5 had nowhere to store a value and the documented rules were unimplementable at the level where overrides are most common                                                                                                                                       | Migration 0026                                                                                                           |
| F-606 | `activateLegalEntity` requires a primary accounting book, `accounting_books` permits exactly one per entity, and the route catalog had no way to create one — an entity created through the API could never become posting-ready                                                                                                                           | Created with the legal entity, in the same transaction, as doc 02's onboarding list describes                            |

**Found in Phase 2** — see [docs/PHASE_2.md](../docs/PHASE_2.md).

Eight are contract gaps: a thing doc 03 requires, with no route to reach it. They were all found the
same way — by trying to drive the posting algorithm end to end and running out of road.

| ID    | Finding                                                                                                                                                                                                       | Resolution                                                                                 |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| F-607 | doc 03 lists `AccountGroup` among its data objects and `accounts.group_id` references it, but no route created one — so the hierarchy the Balance Sheet groups by was unreachable                             | `/account-groups`                                                                          |
| F-608 | `journal_entries.journal_id` is NOT NULL and nothing could create a journal definition, so `createJournalDraft` could never succeed at all                                                                    | `/journal-definitions` — named for entries already owning `/journals`                      |
| F-609 | Step 9 of doc 03's posting algorithm is "validate dimension rules" and `account_dimension_rules` models four of them, but nothing could configure one, so step 9 could never fire                             | `PUT /accounts/{id}/dimension-rules`, replacing the set whole so a removed rule is removed |
| F-610 | doc 01 rule 7 makes versioned, effective-dated posting rules non-negotiable, and there was no route — `posting_rule_versions` could only be filled by hand and `simulatePosting` had nothing to resolve       | `/posting-rules` and `/posting-rules/{id}/versions`, high-risk                             |
| F-611 | doc 03 says "draft can be edited"; only create/submit/approve/post/reverse existed, so a draft with a wrong account could be neither corrected nor discarded                                                  | `PATCH` and `DELETE /journals/{id}`, refusing anything posted before the trigger has to    |
| F-612 | Scheduled reversal cannot be a column on `journal_entries`: 0006's trigger permits exactly one UPDATE on a posted entry (POSTED → REVERSED), so the date could be set only before posting and never cancelled | `scheduled_reversals`, a separate aggregate with its own lifecycle (0029)                  |
| F-613 | Phase 2's fifth exit criterion is about opening balances, and `opening_balance_sets` sat unrouted in the Phase 9 migration file                                                                               | `/opening-balance-sets`, posting through the ordinary posting service                      |
| F-614 | The fourth exit criterion requires the read model to be rebuildable and identical, and nothing rebuilt it — the criterion had no subject                                                                      | `POST /ledger/projections/rebuild`, returning the before/after comparison                  |

Four are schema gaps that only appear when the kernel runs (migration 0029):

| ID    | Finding                                                                                                                                                                                            | Resolution                                                                               |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| F-615 | `journal_line_dimensions` pins its dimension and value to a legal entity but not to its own **line**, so entity B's cost centre could be attached to entity A's posted line. F-003, one level down | Composite foreign keys carrying the scope                                                |
| F-616 | Five ledger tables carry no `tenant_id`, so 0024-style row-level security could not be extended to them at all                                                                                     | Added nullable, backfilled from the parent, set NOT NULL, then policies                  |
| F-617 | `accounts` and `journal_entries` have no `version`, yet the contract gives both PATCH routes an `If-Match` — optimistic concurrency had nothing to compare                                         | `version` columns and `assertVersion`/`etag` in `@acct/domain`                           |
| F-618 | The RLS coverage check in `db:verify` was a hardcoded list of six tables, so every table added afterwards was unprotected _and_ reported as covered                                                | `rls_protected_tables`; the check is now a FULL OUTER JOIN against it, failing both ways |

Four are the ones worth reading. Each was a control that existed, was believed, and did nothing:

| ID    | Finding                                                                                                                                                                                                                                                                                                       | Resolution                                                                        |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| F-619 | 0006 revokes UPDATE/DELETE on `journal_lines` from the application to protect posted history. A privilege is not row-aware, so it also made **drafts** unwritable — doc 03's "draft can be edited" was impossible. 0006's own comment named the answer and it had never been written                          | `ledger_clear_draft_lines` / `ledger_discard_draft`, SECURITY DEFINER (0032)      |
| F-620 | The scheduler asks "what is due today" across all tenants. Under RLS, a connection with no `app.tenant_id` sees nothing, so the query returned empty and the feature silently did nothing                                                                                                                     | Two read-only claim functions returning ids only, `FOR UPDATE SKIP LOCKED` (0033) |
| F-621 | 0032's tenant check read `app_rls_bypassed()`, which is `pg_has_role(current_user, …)` — and inside a SECURITY DEFINER function `current_user` is the **definer**. It was always true, so any authenticated user could discard any tenant's draft by id. The guard scenario reported a pass until fixed twice | The exemption removed, not repaired (0034), plus an assertion that it stays gone  |
| F-622 | `GET /health` answered `"state":"down"` on every call since ADR-0002 introduced `app_runtime`: the readiness probe's migration check began with `CREATE TABLE IF NOT EXISTS`, and the runtime role has no CREATE on schema public. Two phases shipped that way, because the endpoint had no test at all       | The read path made read-only, and `health.test.ts` over HTTP as the runtime role  |

**Found in Phase 3** — see [docs/PHASE_3.md](../docs/PHASE_3.md).

Seven are schema gaps that only appear when a document has to move (migration 0036). Files 0007–0010
are correct about what they cover; these are the things that show up between the covered parts:

| ID    | Finding                                                                                                                                                                                                                                                                                     | Resolution                                                                                                                               |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| F-701 | `quotes` has totals and no lines, but doc 04 gives a quote line items and requires conversion "preserving source link" — so a quote was a number with no content and conversion had nothing to copy                                                                                         | `quote_lines`, shaped like `sales_order_lines`: non-posting, so no revenue account and no snapshot                                       |
| F-702 | There is no goods receipt anywhere. `bill_matches.goods_receipt_line_id` points at `inventory_movements`, which arrives in Phase 5, and `service_receipts` has no lines — so **three-way matching, a Phase 3 exit criterion, had no receipt side**                                          | `goods_receipts` / `goods_receipt_lines`. A receipt is a procurement fact; the stock move is one of its consequences, not the same thing |
| F-703 | A write-off cannot reduce an invoice's open balance: the CHECK is `amount_due = total - amount_paid` and `ar_write_offs` reaches neither term. doc 04's "AR open-item total equals AR GL control account across normal, credit, write-off and FX scenarios" was unreachable by construction | `amount_credited` and `amount_written_off` columns, and the CHECK rewritten across all four terms                                        |
| F-704 | A credit note cannot be applied to an invoice, nor a vendor credit to a bill. Both tables carry `amount_applied` and nothing could move it                                                                                                                                                  | `credit_note_applications` / `vendor_credit_applications`, with deferred over-application assertions                                     |
| F-705 | `files` has no upload state. The row is created by `/files/upload-url` before any bytes exist, so an abandoned upload is indistinguishable from a real attachment — including to the malware scanner                                                                                        | `upload_state` enum: PENDING / COMPLETED / ABANDONED                                                                                     |
| F-706 | Order and quote lines carry `tax_amount` with no `tax_code_id`, so doc 04's tax preview could not record what produced it and conversion to an invoice lost the treatment                                                                                                                   | `tax_code_id` on both line tables                                                                                                        |
| F-707 | `invoice_lines` has no `sales_order_line_id`, so partial invoicing could not maintain `sales_order_lines.quantity_invoiced`                                                                                                                                                                 | The link column, and the quantity maintained through it                                                                                  |

Eleven are contract gaps, found the same way Phase 2's were — by trying to drive doc 04, doc 05 and
doc 07 end to end and running out of road:

| ID    | Finding                                                                                                                                                                                                                                             | Resolution                                                                                                          |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| F-708 | doc 04's customer configuration — payment terms, credit limit, AR/revenue defaults, collection owner — is stated as configuration, and the catalog had create and list only. A credit limit that can be set once and never revised is not a control | `GET`/`PATCH /customers/{id}` and `/vendors/{id}`                                                                   |
| F-709 | doc 04 requires quote conversion "preserving source link" and nothing converted one                                                                                                                                                                 | `POST /quotes/{id}/convert`                                                                                         |
| F-710 | `sales_order.confirmed` is a canonical event and the lifecycle reaches CONFIRMED, but no route produced either — an order could be created and never leave DRAFT                                                                                    | `POST /sales-orders/{id}/confirm`                                                                                   |
| F-711 | `credit_notes.status` starts DRAFT and `credit_note.posted` is canonical, but only create and list existed — a credit note could never reach the ledger, and the same held for vendor credits                                                       | `post` and `apply` on both                                                                                          |
| F-712 | doc 05 routes requisition approval by amount, dimension or category and `purchase_requisition.approved` is canonical, but nothing produced it — so nothing could become a purchase order                                                            | `POST /purchase-requisitions/{id}/approve`                                                                          |
| F-713 | Receiving goods before the bill arrives is an accounting event (Dr destination, Cr GRNI). `accounting_policies.grni_account_id` exists for it and `goods_receipt.posted` is canonical; nothing posted one                                           | `POST /goods-receipts/{id}/post`                                                                                    |
| F-714 | `expense_claim_status` models seven states and three of its transitions are canonical events; the catalog had create and list, so a claim could be typed and never moved                                                                            | `submit`, `approve` (never by the claimant), `post`                                                                 |
| F-715 | doc 05 step 7 tracks SENT/PENDING/CONFIRMED/FAILED and `payment_status` carries all four, but nothing could move a payment out of SENT — a payment sent to the bank stayed sent forever                                                             | `POST /vendor-payments/{id}/confirm`; a failure reverses the allocations                                            |
| F-716 | doc 05 lets a payment run skip a vendor on hold, and there was no hold                                                                                                                                                                              | `/payment-holds`, released rather than deleted — who held payment and who released it is what an investigation asks |
| F-717 | doc 07 gives a tax period its own OPEN → PREPARING → READY → FILED → AMENDED lifecycle, separate from the accounting period because jurisdictions file on their own cadence, and it had no route                                                    | `/tax-periods`, with close refusing while any transaction in range is unposted                                      |
| F-718 | doc 21 requires attachment upload **and** download; `GET /files/{id}` returns metadata only, so the bytes were unreachable                                                                                                                          | `GET /files/{id}/download-url`, refusing a file whose scan has not returned CLEAN                                   |

Plus the guard doc 01 rule 4 implies for every subledger document and only the ledger had: a posted
invoice's financial values are accounting facts, so `guard_posted_document_immutable` now freezes
them. It reads its frozen-column list from a string, which means a typo would silently freeze
nothing — so migration 0036 checks every name against `information_schema.columns` and refuses to
apply if one does not exist.

And one more of the kind that is worth reading, found while building the tax return:

| ID    | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Resolution                                                                                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F-719 | 0008 says in its own comment "A FILED return is an immutable snapshot; doc 07 forbids silently rewriting it" — and attaches the guard to **DELETE only**. Nobody deletes a tax return; the way a filed return gets rewritten is an UPDATE, which was permitted. Same shape as F-619 and F-621: a control that exists, is believed, and does nothing on the path that matters                                                                                                                                                                                                                                                                                               | Migration 0037 adds the UPDATE half, permitting the single transition doc 07 allows (FILED → AMENDED, status and nothing else), with guard scenarios T42–T44                                                                                                                                                                               |
| F-722 | Nothing creates a vendor payment. `allocateVendorPayment` and `confirmVendorPayment` both act on a `vendor_payments` row and no method produces one — two-thirds of a lifecycle with the entry point missing, the same shape as F-703                                                                                                                                                                                                                                                                                                                                                                                                                                      | `ApService.createPayment`, mirroring `createReceipt`: create, allocate and post in one transaction                                                                                                                                                                                                                                         |
| F-723 | **Settlement never reached the general ledger.** Seven things post a journal — invoice, credit note, receipt creation, bill, goods receipt, vendor credit, expense claim — and no settlement does. Applying previously-unapplied cash moved `amount_paid` with no entry; `executePaymentRun` paid bills and left the AP control account untouched; and the realized FX that ADR-0007 recognises per allocation was computed, stored on the allocation row, returned in the response and never posted. Phase 3's first exit criterion — "AR and AP aging totals reconcile to their GL control accounts" — therefore failed by construction the first time anything was paid | `CUSTOMER_RECEIPT_ALLOCATED`, `VENDOR_PAYMENT_POSTED` and `VENDOR_PAYMENT_ALLOCATED` posting rules, the realized gain or loss on the same journal as the allocation that caused it                                                                                                                                                         |
| F-721 | `useResource` — the hook behind every screen in the application — declared its effect as `useEffect(…, [deps, nonce])`. Every call site passes an array literal, so the dependency list held a fresh identity on each render: the effect re-ran after each commit, each completed fetch set state, and that commit started the next fetch. A continuous refetch loop paced by network latency, on every Phase 1 and Phase 2 screen, whose only symptom was traffic                                                                                                                                                                                                         | `[...deps, nonce]` — which is what the hook's own comment already claimed it did                                                                                                                                                                                                                                                           |
| F-720 | `tax_transactions.tax_period_id` exists and carries a composite foreign key, and nothing ever set it. Every period question therefore fell back to a date range, and a posting whose date lands in an already-filed period was indistinguishable from an on-time one — so a return could silently re-report or silently drop it                                                                                                                                                                                                                                                                                                                                            | The reporting period is decided when the transaction is written: the period containing the posting date if it can still accept facts, otherwise the earliest later one that can (doc 07's rule for a late transaction). 0036 revokes UPDATE on the table, so the stamp is permanent and membership is a column rather than a snapshot diff |

### Found by the exit-criteria proofs

The six Phase 3 exit criteria are tested over HTTP against the assembled application — real routes,
real authorization, real database. They found five defects in code that had **121 passing subledger
unit tests and 202 passing API tests**, because a unit test exercises a pure line builder and an
authorization test receives its 403 before the service is ever reached. F-725 is the one worth
sitting with: until that suite existed, no subledger document had ever been posted to a database,
and nothing in 562 green tests said so.

| ID    | Finding                                                                                                                                                                                                                                                                                                                                                                                                     | Resolution                                                                                                                                                                                                                                                    |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-724 | A failed vendor payment reversed its allocations and left the journal standing. doc 05 step 7 has the bank rejecting a sent payment; the AP control account had already been debited and the cash account credited, so a rejection left the entity reporting money it still held as spent                                                                                                                   | The FAILED branch posts a mirrored compensating entry carrying `reversalOfId`, per the standing constraint that a posted fact is corrected only by reversal                                                                                                   |
| F-725 | **No subledger document could post at all.** `journal_entries.source_event_id` is a `uuid` with a partial unique index; every subledger posting passed a readable name — `invoice.posted:<id>` — so every invoice, credit note, receipt, bill, goods receipt, vendor credit, expense claim and settlement failed on `invalid input syntax for type uuid`. Phase 3's entire posting layer was non-functional | `accountingEventId()` hashes the name to an RFC 9562 v5 UUID under a fixed namespace at the single choke point in `DocumentPostingService`. Call sites keep the readable name; replay still collides on the unique index, which is the whole reason it exists |
| F-726 | Document numbering was written twice — privately in `ar.service.ts` and again in `ap.service.ts` — against `number_sequences.next_value`, a column that does not exist. Both copies also skipped the `number_allocations` audit row that doc 02's "a voided number stays visible in the sequence" depends on, and the `{YYYY}` reset rules                                                                  | Both delegate to `@acct/ledger`'s `allocateNumber`, which has allocated journal numbers correctly since Phase 2 under ADR-0004 §1's `SELECT … FOR UPDATE`                                                                                                     |
| F-727 | Purchase orders returned calculated lines with no row ids, and goods receipts returned no lines at all — so a caller could not name the line ids that three-way matching takes as input. The drill chain was unreachable over HTTP even though every table behind it was correct                                                                                                                            | Both create responses now return the persisted lines                                                                                                                                                                                                          |
| F-728 | The general ledger report returned `entry_number` and no `journal_entry_id`, so a report row could not be followed back to its entry — the last hop of the drill chain, and the one an auditor actually takes                                                                                                                                                                                               | `e.id AS journal_entry_id` in the GL projection                                                                                                                                                                                                               |

### Found by reading Gate H's own output

Three more, found while re-running the benchmark to write this phase up. They share a shape with
F-620 and F-622 — a gate that reported success, believed, about something it was not measuring:

| ID    | Finding                                                                                                                                                                                                                                                                                                                                                                                                         | Resolution                                                                                                                                                                                                    |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-729 | `bench.ts` defaulted `--phase` to a hardcoded `2`, so Gate H kept reporting "subject arrives in phase 3" about the two aging workloads Phase 3 had just delivered. The exact defect Phase 2 fixed in `dod:check`, in a second place: a copy of the delivered-phase list that nobody updates because nothing fails when it is stale                                                                              | The default reads `DELIVERED_PHASES` from `apps/api/src/common/operation.ts`, the same declaration `dod:check` parses                                                                                         |
| F-730 | With the phase gate opened, both aging workloads reported **pass at 1.2ms against a 2,000ms target** — over two empty tables. `profiles.ts` had declared 2,000 invoices for `small` and 50,000 for `reference` since Phase 0, to be seeded "once the phase that owns those tables exists"; the phase arrived and the seeder did not follow. A benchmark measuring an empty result is not a fast benchmark       | `seed/subledger.ts`: contacts, invoices and bills with a status mix that makes the query's filter discriminate and due dates spanning all five buckets. The seeder now **fails** if it produced no open items |
| F-731 | The identical dataset measures `trial-balance-one-period` at **22.4ms** freshly seeded and **3.8ms** once analysed — a 6× swing decided by whether autovacuum happened to reach the table. ADR-0009 §1's whole promise is that "a baseline recorded on one machine is comparable with a run on another", and every Gate H number recorded before this was measured against whatever statistics that machine had | The seeder runs `ANALYZE` as its last step, and the `small` baseline was re-recorded against an analysed database                                                                                             |

F-730 also exposed why it survived: a full seed is not re-runnable — the foundation and ledger mint
fixed ids from a fixed stream and collide with themselves — so adding 4,000 rows meant destroying the
volumes and regenerating 20,000 journal lines first. `pnpm db:seed -- --only subledger` now resumes a
single stage, which is what makes the next phase's seed data cheap enough to actually add.

Two SQL type defects travelled with them, both from the same habit of trusting a cast to arrive late.
`journals.journal_type` is `text` with a CHECK, not an enum, so `$7::journal_type` failed at plan
time for every document that had to open its own journal. And `coalesce($11, 0)::numeric` infers the
parameter as **integer** from the literal `0` and casts the result afterwards — so an ordinary
`'5.00'` unit price failed to parse before the cast was ever reached. The cast belongs inside:
`coalesce($11::numeric, 0)`.

## Layout

```
contracts/
  events.yaml                     canonical event contract
  openapi.yaml                    canonical route contract
  schema/
    0000_extensions_and_types     extensions, 61 enums, shared guard functions
    0001_platform                 tenancy, outbox, idempotency, audit, files, retention
    0002_identity                 users, sessions, permission registry, roles, memberships
    0003_organization             entities, branches, currencies, sequences, accounting policy
    0004_periods_books            fiscal years, periods, books, period resolution
    0005_ledger                   chart, dimensions, journals, posting rules       <- kernel
    0006_ledger_guards            immutability + balance assertion                 <- Gate C
    0007_contacts                 customers/vendors, bank details (versioned)
    0008_tax_localization         determination, versioning, subledger, returns, e-invoice
    0009_ar                       quotes, orders, invoices, credits, receipts, allocations
    0010_ap                       requisitions, POs, bills, matching, payment runs, expenses
    0011_banking                  connections, transactions, rules, matching, reconciliation
    0012_inventory                items, warehouses, movements, cost layers, landed cost
    0013_assets                   categories, assets, books, depreciation, disposal
    0014_projects_budgets         projects, time, budgets, commitments, forecasts
    0015_fx_consolidation         rates, remeasurement, intercompany, consolidation
    0016_reporting_close          report definitions, snapshots, close, reconciliations
    0017_workflow_tasks           workflows, approvals, SoD, delegation, tasks, notifications
    0018_ai                       model configs, budgets, decisions, agent actions, evaluation
    0019_integrations_migration   connectors, webhooks, import, opening balances, cutover
    0020_saas_admin               plans, entitlements, usage, support access, PII inventory
    0021_localization_bd          Bangladesh pack (Phase 10, out of first-release scope)
    tests/
      0006_guards_test.sql        the 15-test guard suite above
```

## Rules

1. **`project plan/` is never modified.** It is the immutable v1.0 handoff. Where this directory
   disagrees with it, this directory wins and the audit finding explains why.
2. **Event types are immutable once published.** A breaking payload change is a new `event_version`.
   The `forbidden:` block in `events.yaml` records every superseded name so a developer grepping the
   old prose lands on the canonical form.
3. **The permission registry is generated** from `openapi.yaml` `x-permission` values. CI fails if a
   route declares a permission absent from the registry, or a role references one that does not
   exist. This is what makes Gate B's "authorization tests cover every mutation route" provable.
4. **Migrations are forward-only.** Rollback is a new forward migration (ADR-0008).
5. **Anything Gate C, D, E or G tests is raw SQL, not Prisma** (ADR-0008 §2).

## Reproducing the verification

```bash
docker run -d --name acct_schema_check -e POSTGRES_PASSWORD=verify \
  -e POSTGRES_DB=acct -p 55432:5432 postgres:16

for f in contracts/schema/*.sql; do
  docker exec -i acct_schema_check psql -U postgres -d acct -v ON_ERROR_STOP=1 -q -f - < "$f"
done
docker exec -i acct_schema_check psql -U postgres -d acct -f - \
  < contracts/schema/tests/0006_guards_test.sql
```
