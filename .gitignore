node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example

.pnpm-store/
.turbo/
.vitest/

# Benchmark output is machine-specific; committed baselines live in bench/baselines/
bench/results/

.DS_Store
Thumbs.db
*.log

# Nothing but the v1.0 handoff belongs in "project plan/" — a 60MB Go installer
# was once swept in by `git add -A`. The folder is the immutable specification
# package and every file in it is hash-verified against its MANIFEST.
project plan/*.msi
project plan/*.exe
project plan/*.zip
*.msi
